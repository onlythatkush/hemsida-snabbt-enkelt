/**
 * Build marker so we can verify exactly which bundle a domain is serving.
 * Bump when publishing a production build.
 */
export const BUILD_ID = "8f6ef4d1";
export const BUILD_LABEL = `build ${BUILD_ID}`;
